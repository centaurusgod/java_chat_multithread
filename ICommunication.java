// package graphics;

// public package graphics;

interface ICommunication {
    public void sendMessage(String message);
    public String receiveMessage();
   // public boolean initializeConnection(int receiverPort, int senderPort);
}
